<% if  @cantidadfacturas == 0 %>
  <h3>No has facturado nada Aun!..</h3>
<% else %>
<table class="table table-hover table-print">
    <thead>
      <tr>
        <th></th>
        <th>VENTANILLA</th>
        <th>HOSPITALIZACIÓN</th>
        <th>TOTAL</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>Facturas:</td>
        <td><%= @cantidad_ventanilla %></td>
        <td><%= @cantidad_hospitalizacion %></td>
        <td><%= @cantidadfacturas %></td>
      </tr>
      <tr>
        <td>Subtotal:</td>
        <td><%= redondear(@ventanilla_subtotal) %></td>
        <td><%= redondear(@hospitalizacion_subtotal) %></td>
        <td><%= redondear(@total_subtotal) %></td>
      </tr>
      <tr>
        <td>Iva:</td>
        <td><%= redondear(@ventanilla_iva) %></td>
        <td><%= redondear(@hospitalizacion_iva) %></td>
        <td><%= redondear(@total_iva) %></td>
      </tr>
      <tr>
        <td>Total:</td>
        <td><%= redondear(@total_ventanilla) %></td>
        <td><%= redondear(@total_hospitalizacion) %></td>
        <td><strong><%= redondear(@totalfacturas) %></strong></td>
      </tr>
    </tbody>
</table>
<% end %>
