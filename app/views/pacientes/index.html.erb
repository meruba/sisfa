<div class="row title-history">
	<h3 class="text-center">Administracion de Pacientes</h3>
</div>
<div class="row info-paciente">
	<div class="acciones">
		<div class="well well-sm">
			<strong><small>Crear Historia Clinica:</small></strong>
			<%=link_to("Militar", pacientes_militar_path, class: "nuevo_new btn-xs")%>
			<%=link_to("Familiar", pacientes_familiar_path, class: "nuevo_new btn-xs")%>
			<%=link_to("Civil", pacientes_civil_path, class: "nuevo_new btn-xs")%>
			<%=link_to("Descargar Pacientes", reporte_pacientes_path(:format => "xls"), class: "btn btn-success btn-xs",data: {disable_with: "<i class='fa fa-spinner fa-spin'></i> Descargando..."})%>
		</div>
	</div>
	<div class="table-historias" id="flip-scroll">
		<%= render 'pacientes' %>
	</div>
</div>
