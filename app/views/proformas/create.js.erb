<%- if @proforma.errors.any? %>
	<%- @proforma.errors.full_messages.each do |msg|%>
		alertify.error("<%=j msg %>");
	<%- end %>
<%- else %>
	alertify.success("Receta enviada a farmacia");
	setTimeout(function () { window.close();}, 3000);
<%- end %>
